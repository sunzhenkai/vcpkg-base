cpp-common provides CMake targets:

find_package(cppcommon CONFIG REQUIRED)
target_link_libraries(main PRIVATE cppcommon::cppcommon)

find_package(objectstorage CONFIG REQUIRED)
target_link_libraries(main PRIVATE cppcommon::objectstorage)
