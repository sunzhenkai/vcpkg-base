diff --git a/CMakeLists.txt b/CMakeLists.txt
index 609d3bd..71dcf26 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -3,5 +3,20 @@ project(apollo C)
 
 set(CMAKE_C_STANDARD 99)
 
-add_library(apollo SHARED apolloclient.c global.c)
-target_link_libraries(apollo curl json-c)
\ No newline at end of file
+SET(LIBRARY_NAME apollo-client)
+INCLUDE(GNUInstallDirs) # CMAKE_INSTALL_INCLUDEDIR / CMAKE_INSTALL_LIBDIR / ...
+find_package(CURL REQUIRED)
+
+add_library(${LIBRARY_NAME} STATIC apolloclient.c global.c)
+target_link_libraries(${LIBRARY_NAME} CURL::libcurl json-c)
+
+INSTALL(TARGETS ${LIBRARY_NAME}
+        EXPORT ${LIBRARY_NAME}
+        LIBRARY DESTINATION ${CMAKE_INSTALL_LIBDIR}
+        ARCHIVE DESTINATION ${CMAKE_INSTALL_LIBDIR}
+        RUNTIME DESTINATION ${CMAKE_INSTALL_BINDIR})
+INSTALL(DIRECTORY . DESTINATION "${CMAKE_INSTALL_INCLUDEDIR}/apollo-client" FILES_MATCHING PATTERN "*.h")
+INSTALL(EXPORT ${LIBRARY_NAME}
+        DESTINATION share/${LIBRARY_NAME}
+        FILE ${LIBRARY_NAME}Config.cmake
+        NAMESPACE ${LIBRARY_NAME}::)
